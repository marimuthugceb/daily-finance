<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.jbr.dailyfinance"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" android:icon="@drawable/icon">
        <activity android:name="TicketList"
                  android:label="@string/ticket_list_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ProductList"
                  android:label="@string/app_name"/>
        <activity android:name="EditProduct"
                  android:label="@string/app_name"/>
        <activity android:name="NewTicket"
                  android:noHistory="true"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.Dialog"/>
        <activity android:name="EditCategory"
                  android:noHistory="true"
                  android:label="@string/edit_category"
                  android:theme="@android:style/Theme.Dialog"/>
        <activity android:name="TicketProductPrice"
                  android:label="@string/ticket_product_price_title"
                  android:theme="@android:style/Theme.Dialog"/>
        <activity android:name="Ticket"
                  android:label="@string/app_name"/>
    </application>
</manifest> 
